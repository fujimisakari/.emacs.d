;;; 54-dockerfile-mode.el --- dockerfile-mode設定 -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:

(add-to-list 'auto-mode-alist '("Dockerfile.*" . dockerfile-mode))

;;; 54-dockerfile-mode.el ends here
